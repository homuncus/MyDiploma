/* global authId, window, ADM, Audio */

// const io = require('socket.io-client');

// const socket = io.connect(window.location.origin, {
//   transports: ['polling'],
// });

// socket.on('connect', () => {
//   socket.on('message', (msg) => {
//     ADM.notification.show({ title: 'Soket notification', message: msg }, { delay: 2000 });
//     // (new Audio('/admin/media/notification.mp3')).play();
//   });
// });

// module.export = {};
